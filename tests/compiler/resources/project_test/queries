MATCH (a:person) RETURN a.fName ORDER BY a.fName LIMIT 1 + 1; # LIMIT
MATCH (a:organisation) RETURN *; # RETURN_STAR
MATCH (a:organisation) RETURN a.*; # VERTEX_STAR
MATCH (a:person)-[e:knows]->(b:person) WHERE a.ID =7 RETURN e.*; # EDGE_STAR
// MATCH (a:organisation) RETURN a.state.*; # PROPERTY_STAR
// MATCH (a:person)-[e:knows]->(b:person) RETURN e.date; # E_DATE
// MATCH (a:person)-[e:knows]->(b:person) WHERE b.age=20 RETURN b.age * 2; # AGE_MULTI
MATCH (a:person) WHERE a.fName='Alice' RETURN a.age, a.age AS k; # DUPLICATE_EDGE
// MATCH (a:person) RETURN a.birthdate; # A_BIRTHDAY
// MATCH (p:person)-[e:meets]->(p1:person) RETURN e.data; # E_DATA
// RETURN "hEl \'\"\\"; # SPECIAL_CHAR

